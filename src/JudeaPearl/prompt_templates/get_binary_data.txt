We have just run a simulation of the following scenario: "{{scenario_description}}", with the following human agents: {{relevant_agents}}
and we are trying to extract data from the transcript of the simulation for analysis of this variable: {{variable_name}}.
We know that the variable is a binary variable.
We have asked this agent: {{agent}}, the following question to get the results: {{question}}.
The agent has provided this answer: {{answer}}.
The ordinal variable can take on these values: {{levels}}, which will map to these numeric value or data analysis: [0,1]
Your task is to extract the correct binary numeric value from the agent's answer so we can run a regression on the results of our simulation.
Please extract only the number (which we will convert directly into an int) of 0 or 1 that corresponds to the agents response of the levels here:{{levels}}.
Please respond with "NA" if the data is missing.

IMPORTANT: You MUST respond with exactly "0", "1", or "NA" in the "answer" field. Map any text responses as follows:
- "yes", "true", "agree", or the second level → "1"
- "no", "false", "disagree", or the first level → "0"
- missing or unclear → "NA"

Examples:
- Agent answered "yes" → {{ '{' }}"answer":"1", "explanation": "Agent said yes, mapping to 1"{{ '}' }}
- Agent answered "no" → {{ '{' }}"answer":"0", "explanation": "Agent said no, mapping to 0"{{ '}' }}
- Agent answered "1" → {{ '{' }}"answer":"1", "explanation": "Agent directly provided numeric value 1"{{ '}' }}

Format your response as a json in this form:
{{ '{' }}"answer":"0, 1, or NA",
"explanation": "short explanation for extraction of value"{{ '}' }}
